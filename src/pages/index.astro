---
import BaseLayout from '../layouts/BaseLayout.astro';
import HeroVisual from '../components/HeroVisual.astro';
import CapabilitiesStory from '../components/CapabilitiesStory.astro';
import CTA from '../components/CTA.astro';
import {
  ArrowRight,
  Bot,
  Building2,
  CodeXml,
  Compass,
  Cpu,
  Factory,
  GraduationCap,
  Handshake,
  HeartPulse,
  Layers,
  Landmark,
  Plane,
  Rocket,
  Search,
  Shield,
  ShoppingCart,
  Sparkles,
  Target,
  TrendingUp,
  Truck,
  Zap,
} from 'lucide-react';
import { solutions } from '../data/site';

const leadershipMetrics = [
  { value: '20+', label: 'Years Technology Leadership', icon: Cpu, iconBg: '#dbeafe', iconColor: '#1d4ed8' },
  { value: '20+', label: 'Years Growth Marketing Experience', icon: Target, iconBg: '#ffedd5', iconColor: '#c2410c' },
  { value: 'Global', label: 'Client Experience', icon: Landmark, iconBg: '#dcfce7', iconColor: '#15803d' },
  { value: 'One Roof', label: 'AI, Software, Marketing & Automation', icon: Handshake, iconBg: '#f3e8ff', iconColor: '#7e22ce' },
];

const featuredWork = [
  { slug: 'ai-video-campaign-real-estate-developer', category: 'AI Creative', title: 'AI Video Campaign for Real Estate Developer', metric: '2x', result: 'Lead Volume from Video Channels', image: 'https://images.pexels.com/photos/323780/pexels-photo-323780.jpeg?auto=compress&cs=tinysrgb&w=800&h=400&fit=crop' },
  { slug: 'saas-platform-assessment-company', category: 'Software', title: 'Custom SaaS Platform for Assessment Company', metric: '< 5 days', result: 'Results Turnaround (from 3 weeks)', image: 'https://images.pexels.com/photos/34069/pexels-photo.jpg?auto=compress&cs=tinysrgb&w=800&h=400&fit=crop' },
  { slug: 'b2b-lead-generation-campaign', category: 'Marketing', title: 'B2B Lead Generation Campaign', metric: '35–45%', result: 'Lower Cost per Qualified Lead', image: 'https://images.pexels.com/photos/6930549/pexels-photo-6930549.jpeg?auto=compress&cs=tinysrgb&w=800&h=400&fit=crop' },
  { slug: 'enterprise-logistics-management-platform', category: 'Software', title: 'Enterprise Logistics Management Platform', metric: '90s%', result: 'On-Time Delivery (from 78%)', image: 'https://images.pexels.com/photos/4480797/pexels-photo-4480797.jpeg?auto=compress&cs=tinysrgb&w=800&h=400&fit=crop' },
  { slug: 'ai-chatbot-automation-ecommerce-brand', category: 'Automation', title: 'AI Chatbot Automation for E-Commerce Brand', metric: '55–65%', result: 'Queries Resolved Without Human', image: 'https://images.pexels.com/photos/5980898/pexels-photo-5980898.jpeg?auto=compress&cs=tinysrgb&w=800&h=400&fit=crop' },
  { slug: 'ecommerce-growth-campaign', category: 'Marketing', title: 'E-Commerce Growth Campaign', metric: '1.2% → 2.2%', result: 'Site-Wide Conversion Rate', image: 'https://images.pexels.com/photos/5632397/pexels-photo-5632397.jpeg?auto=compress&cs=tinysrgb&w=800&h=400&fit=crop' },
];

const industryItems = [
  { name: 'Healthcare',                icon: HeartPulse,    bg: '#ef4444', from: '#ef4444', to: '#f97316' },
  { name: 'Education',                 icon: GraduationCap, bg: '#d97706', from: '#f59e0b', to: '#fbbf24' },
  { name: 'E-Commerce',                icon: ShoppingCart,  bg: '#059669', from: '#10b981', to: '#34d399' },
  { name: 'Logistics & Transportation',icon: Truck,         bg: '#2563eb', from: '#3b82f6', to: '#38bdf8' },
  { name: 'Travel & Hospitality',      icon: Plane,         bg: '#0891b2', from: '#06b6d4', to: '#0ea5e9' },
  { name: 'Real Estate',               icon: Building2,     bg: '#0f766e', from: '#059669', to: '#14b8a6' },
  { name: 'Finance & FinTech',         icon: Landmark,      bg: '#1e40af', from: '#1e40af', to: '#3b82f6' },
  { name: 'Manufacturing',             icon: Factory,       bg: '#c2410c', from: '#ea580c', to: '#f59e0b' },
  { name: 'Startups',                  icon: Rocket,        bg: '#be123c', from: '#e11d48', to: '#fb923c' },
];

const processSteps = [
  { title: 'Discover', icon: Search, tone: 'purple', text: 'We understand your business, audience, challenges, and growth goals.' },
  { title: 'Strategize', icon: Target, tone: 'green', text: 'We design a clear roadmap across technology, marketing, AI, and automation.' },
  { title: 'Build', icon: CodeXml, tone: 'blue', text: 'We create the required digital products, campaigns, systems, and content.' },
  { title: 'Launch', icon: Rocket, tone: 'orange', text: 'We deploy, test, optimize, and prepare your business for market execution.' },
  { title: 'Scale', icon: TrendingUp, tone: 'navy', text: 'We continuously improve performance, conversions, automation, and growth.' },
];

const solutionMeta = [
  { icon: Rocket, bg: '#dbeafe', color: '#1d4ed8', accent: '#3b82f6' },
  { icon: TrendingUp, bg: '#dcfce7', color: '#15803d', accent: '#10b981' },
  { icon: Bot, bg: '#f3e8ff', color: '#7e22ce', accent: '#a855f7' },
  { icon: Sparkles, bg: '#ffedd5', color: '#c2410c', accent: '#f97316' },
];

const whyReasons = [
  { icon: Compass, title: 'Strategy first', text: 'Every engagement starts with the why — not the tactics.', tone: 'blue' },
  { icon: Bot, title: 'AI first', text: 'AI embedded in every solution, not bolted on after.', tone: 'purple' },
  { icon: Layers, title: 'Tech + marketing together', text: 'One team across the full growth stack, end to end.', tone: 'teal' },
  { icon: Shield, title: 'Leadership driven', text: 'Senior team, end-to-end ownership, no handoffs.', tone: 'green' },
  { icon: Target, title: 'Outcome focused', text: 'Measured by business metrics, not deliverables.', tone: 'orange' },
  { icon: Zap, title: 'Long-term partnership', text: 'Relationships that span years, not projects.', tone: 'pink' },
];
---
<BaseLayout title="AI-Powered Growth & Innovation Company | Biznyss" description="Biznyss helps organizations launch, scale, automate, and innovate through AI, software, creative production, and growth marketing.">
  <section class="hero">
    <div class="container hero-grid">
      <div class="hero-copy reveal">
        <span class="eyebrow">AI-powered growth ecosystem</span>
        <h1>Transforming businesses through <em>AI, technology</em> & growth</h1>
        <p class="lead">Biznyss helps organizations launch digital products, create AI-powered marketing campaigns, automate operations, and accelerate growth through intelligent technology and performance-driven execution.</p>
        <div class="buttons"><a class="btn btn-primary" href="/contact">Book Strategy Call</a><a class="btn btn-secondary" href="/case-studies">Explore Case Studies</a></div>
      </div>
      <div class="hero-visual">
        <HeroVisual />
      </div>
    </div>
  </section>

  <section class="leadership-strip">
    <div class="container">
      <div class="leadership-panel">
        {leadershipMetrics.map((metric) => {
          const Icon = metric.icon;
          return (
            <div class="leadership-item">
              <span class="leadership-icon" style={`background:${metric.iconBg};color:${metric.iconColor}`}><Icon size={24} strokeWidth={1.7} aria-hidden="true" /></span>
              <div><strong>{metric.value}</strong><span>{metric.label}</span></div>
            </div>
          );
        })}
      </div>
    </div>
  </section>

  <CapabilitiesStory />

  <section class="section solutions-section">
    <div class="container">
      <div class="section-head solutions-heading"><div><span class="eyebrow">Business solutions</span><h2>Start with the outcome,<br/>not the service.</h2></div><p class="lead">Different stages demand different systems. Choose the ambition that best describes where your business needs to go.</p></div>
      <div class="solution-grid">
        {solutions.map((solution, index) => {
          const Icon = solutionMeta[index].icon;
          return (
            <a href={`/solutions/${solution.slug}`} class="solution-card" style={`--accent:${solutionMeta[index].accent}`}>
              <div class="solution-card-head">
                <span class="solution-icon" style={`background:${solutionMeta[index].bg};color:${solutionMeta[index].color}`}><Icon size={28} strokeWidth={1.7} aria-hidden="true" /></span>
                <span class="solution-num">0{index + 1}</span>
              </div>
              <h3>{solution.name}</h3>
              <p>{solution.summary}</p>
              <span class="solution-audience-tag">{solution.audience}</span>
              <span class="solution-cta">Explore solution <ArrowRight size={14} aria-hidden="true" /></span>
            </a>
          );
        })}
      </div>
    </div>
  </section>

  <section class="section featured-work-section">
    <div class="container">
      <div class="compact-heading">
        <div><span>Featured Case Studies</span><h2>Work Designed to Deliver Business Impact</h2></div>
        <a href="/case-studies">View All Case Studies <ArrowRight size={15} aria-hidden="true" /></a>
      </div>
      <div class="work-grid">
        {featuredWork.map((study) => (
          <a class="work-card" href={`/case-studies/${study.slug}`}>
            <div class="work-art">
              <img src={study.image} alt={study.title} loading="lazy" decoding="async" />
            </div>
            <div class="work-content">
              <span class="work-category">{study.category}</span>
              <h3>{study.title}</h3>
              <strong>{study.metric}</strong>
              <small>{study.result}</small>
              <span class="work-link">View Case Study <ArrowRight size={13} aria-hidden="true" /></span>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>

  <section class="industry-section">
    <div class="container">
      <div class="industry-heading">
        <div>
          <span class="compact-kicker">Industries We Serve</span>
          <h2>Cross-sector perspective.<br/>Context-specific execution.</h2>
        </div>
        <p class="lead">We bring patterns from multiple industries without forcing one industry's playbook onto another.</p>
      </div>
      <div class="industry-carousel">
        <div class="industry-track">
          {[...industryItems, ...industryItems].map(({ name, icon: Icon, bg, from, to }) => (
            <div class="industry-card" style={`--bg:${bg};--from:${from};--to:${to}`}>
              <span class="card-icon-wrap">
                <Icon size={28} strokeWidth={1.7} aria-hidden="true" />
              </span>
              <strong>{name}</strong>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <section class="section process-section">
    <div class="container">
      <div class="process-heading"><span>Our Process</span><h2>From Strategy to Scale</h2></div>
      <div class="process-rail">
        {processSteps.map((step, index) => {
          const Icon = step.icon;
          return (
            <div class:list={['process-step', step.tone]}>
              <span class="process-icon"><Icon size={28} strokeWidth={1.8} aria-hidden="true" /></span>
              <h3>{index + 1}. {step.title}</h3>
              <p>{step.text}</p>
            </div>
          );
        })}
      </div>
    </div>
  </section>

  <section class="section why-section">
    <div class="container">
      <div class="why-header">
        <span class="eyebrow light">Why Biznyss</span>
        <h2>One partner for the systems<br/>that create growth.</h2>
        <p class="lead">Less coordination overhead. More strategic continuity from the boardroom to the build.</p>
      </div>
      <div class="why-bento">
        {whyReasons.map((reason, i) => {
          const Icon = reason.icon;
          return (
            <div class={`why-card why-card-${reason.tone}`}>
              <span class="why-icon"><Icon size={26} strokeWidth={1.7} aria-hidden="true" /></span>
              <h3>{reason.title}</h3>
              <p>{reason.text}</p>
              <span class="why-num">0{i + 1}</span>
            </div>
          );
        })}
      </div>
    </div>
  </section>
  <CTA />
</BaseLayout>

<style>
  .hero {
    position: relative;
    overflow: hidden;
    padding: 178px 0 130px;
    background: linear-gradient(155deg, #060d1c 0%, #091628 55%, #07101f 100%);
  }
  /* Blue glow — top right */
  .hero::before {
    position: absolute;
    width: 900px; height: 900px;
    right: -160px; top: -260px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(59,130,246,.22) 0%, rgba(6,182,212,.08) 42%, transparent 70%);
    content: ""; pointer-events: none;
  }
  /* Orange accent — bottom left */
  .hero::after {
    position: absolute;
    width: 600px; height: 600px;
    left: -180px; bottom: -180px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(233,113,66,.14) 0%, transparent 65%);
    content: ""; pointer-events: none;
  }
  .hero-grid {
    position: relative; z-index: 1;
    display: grid; grid-template-columns: minmax(0,1fr) minmax(0,1fr); align-items: center; gap: clamp(42px,5vw,82px);
    /* subtle dot grid texture */
    --dot: rgba(255,255,255,.055);
    background-image: radial-gradient(var(--dot) 1px, transparent 1px);
    background-size: 32px 32px;
    background-position: 0 0;
  }
  .hero-visual { display: flex; align-items: center; justify-content: center; overflow: visible; }
  .hero :global(.ecosystem) { width: 100%; max-width: 460px; }
  .hero-copy .eyebrow {
    padding: 7px 15px; margin-bottom: 24px; border-radius: 999px;
    color: #60a5fa; background: rgba(96,165,250,.1); border: 1px solid rgba(96,165,250,.25);
    font-size: .8rem; font-weight: 700; letter-spacing: .03em; text-transform: none;
  }
  .hero-copy .eyebrow::before { display: none; }
  .hero-copy h1 {
    max-width: 760px; color: #f1f5f9;
    font-size: clamp(3.15rem, 5.25vw, 5.35rem); font-weight: 700; line-height: 1.02; letter-spacing: -.04em;
  }
  .hero-copy h1 em {
    background: linear-gradient(125deg, #60a5fa 0%, #a78bfa 55%, #38bdf8 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    font-style: normal;
  }
  .hero-copy .lead { max-width: 580px; margin: 28px 0 36px; color: #94a3b8; font-size: clamp(1.05rem, 1.4vw, 1.18rem); font-weight: 400; line-height: 1.7; }
  .hero .buttons .btn-primary { background: #fff; color: #0d1321; border-color: #fff; box-shadow: 0 8px 28px rgba(255,255,255,.15); }
  .hero .buttons .btn-primary:hover { background: #f0f6ff; border-color: #f0f6ff; transform: translateY(-2px); }
  .hero .buttons .btn-secondary { background: transparent; border-color: rgba(255,255,255,.22); color: #e2e8f0; }
  .hero .buttons .btn-secondary:hover { background: rgba(255,255,255,.07); border-color: rgba(255,255,255,.4); }
  .leadership-strip { position: relative; z-index: 3; margin-top: -72px; padding: 0 0 48px; background: linear-gradient(to bottom, transparent 0%, white 18%); }
  .leadership-panel { display: grid; grid-template-columns: repeat(4, 1fr); overflow: hidden; border: 1px solid var(--border); border-radius: 16px; background: white; box-shadow: 0 24px 60px rgba(13,23,32,.14); }
  .leadership-item { min-height: 96px; display: flex; align-items: center; gap: 15px; padding: 20px 28px; border-left: 1px solid var(--border); }
  .leadership-item:first-child { border-left: 0; }
  .leadership-icon { width: 48px; height: 48px; display: grid; flex: 0 0 auto; place-items: center; border-radius: 50%; transition: transform .3s ease; }
  .leadership-item:hover .leadership-icon { transform: translateY(-3px) scale(1.05); }
  .leadership-item strong, .leadership-item span { display: block; }
  .leadership-item strong { color: #0d5bb8; font-size: 1.35rem; font-weight: 700; line-height: 1; letter-spacing: -.025em; }
  .leadership-item div > span { max-width: 145px; margin-top: 7px; color: #374151; font-size: .68rem; font-weight: 600; line-height: 1.35; }
  .solutions-section { padding: 82px 0 72px; }
  .solutions-heading { display: block; max-width: 760px; }
  .solutions-heading .lead { max-width: 660px; margin-top: 24px; }
  .solution-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 20px; }
  .solution-card {
    position: relative; display: flex; flex-direction: column;
    padding: 30px 26px 26px;
    border: 1px solid var(--border); border-radius: 18px;
    background: #fff; box-shadow: 0 4px 16px rgba(13,23,32,.04);
    transition: transform .3s ease, box-shadow .3s ease, border-color .3s ease;
    overflow: hidden;
  }
  .solution-card::before {
    content: ''; position: absolute; top: 0; left: 0; right: 0; height: 4px;
    background: var(--accent); transform: scaleX(0); transform-origin: left; transition: transform .35s ease;
  }
  .solution-card:hover { transform: translateY(-6px); box-shadow: 0 20px 44px rgba(13,23,32,.1); border-color: rgba(0,0,0,.06); }
  .solution-card:hover::before { transform: scaleX(1); }
  .solution-card-head { display: flex; align-items: center; justify-content: space-between; margin-bottom: 22px; }
  .solution-icon { width: 56px; height: 56px; display: grid; place-items: center; border-radius: 16px; transition: transform .3s ease; }
  .solution-card:hover .solution-icon { transform: scale(1.08) rotate(-4deg); }
  .solution-num { color: #cfd8e3; font-size: 1.6rem; font-weight: 800; letter-spacing: -.03em; }
  .solution-card h3 { font-size: 1.3rem; font-weight: 700; letter-spacing: -.02em; }
  .solution-card p { margin-top: 12px; color: var(--muted); font-size: .9rem; line-height: 1.6; flex: 1; }
  .solution-audience-tag { display: inline-block; margin-top: 16px; padding: 5px 12px; border-radius: 999px; background: var(--surface); color: var(--muted); font-size: .72rem; font-weight: 600; }
  .solution-cta { display: flex; align-items: center; gap: 6px; margin-top: 20px; color: var(--accent); font-size: .82rem; font-weight: 700; }
  .featured-work-section { padding: 46px 0 72px; background: white; }
  .compact-heading { display: flex; align-items: end; justify-content: space-between; gap: 30px; margin-bottom: 24px; }
  .compact-heading span, .compact-kicker { display: block; color: #0a5df0; font-size: .66rem; font-weight: 700; letter-spacing: .07em; text-transform: uppercase; }
  .compact-heading h2 { margin-top: 7px; font-size: clamp(1.55rem,2.6vw,2.4rem); letter-spacing: -.03em; }
  .compact-heading > a { display: inline-flex; align-items: center; gap: 7px; color: var(--blue); font-size: .72rem; font-weight: 700; white-space: nowrap; }
  .work-grid { display: grid; grid-template-columns: repeat(6, 1fr); gap: 12px; }
  .work-card { overflow: hidden; border: 1px solid var(--border); border-radius: 10px; background: white; box-shadow: 0 8px 22px rgba(13,23,32,.045); transition: transform .25s ease-out, box-shadow .25s ease-out; }
  .work-card:hover { transform: translateY(-4px); box-shadow: 0 16px 32px rgba(13,23,32,.1); }
  .work-art { position: relative; height: 132px; overflow: hidden; background: #17233d; }
  .work-art img { width: 100%; height: 100%; object-fit: cover; display: block; transition: transform .4s ease; }
  .work-card:hover .work-art img { transform: scale(1.06); }
  .work-content { position: relative; min-height: 205px; display: flex; flex-direction: column; padding: 15px 14px; }
  .work-category { align-self: flex-start; margin-top: -24px; margin-bottom: 13px; padding: 4px 8px; border-radius: 4px; color: #5841ac; background: #eeeafe; font-size: .54rem; font-weight: 700; text-transform: uppercase; letter-spacing: .04em; }
  .work-card:nth-child(2) .work-category, .work-card:nth-child(4) .work-category { color: #16784a; background: #e6f7ed; }
  .work-card:nth-child(3) .work-category, .work-card:nth-child(5) .work-category { color: #bf5a23; background: #fff0e6; }
  .work-card:nth-child(6) .work-category { color: #16784a; background: #e6f7ed; }
  .work-content h3 { min-height: 47px; font-size: .82rem; font-weight: 600; line-height: 1.4; letter-spacing: -.01em; }
  .work-content > strong { margin-top: 13px; color: #0d5bb8; font-size: 1.25rem; font-weight: 700; line-height: 1; }
  .work-content > small { margin-top: 5px; color: var(--muted); font-size: .6rem; font-weight: 500; }
  .work-link { display: flex; align-items: center; gap: 5px; margin-top: auto; padding-top: 14px; color: var(--blue); font-size: .62rem; font-weight: 700; }
  .industry-section { padding: 65px 0 80px; background: #fbfcfe; }
  .compact-kicker { margin-bottom: 12px; }
  .industry-heading { display: block; max-width: 790px; margin-bottom: 30px; }
  .industry-heading h2 { font-size: clamp(2rem,3.5vw,3.45rem); }
  .industry-heading .lead { max-width: 650px; margin-top: 22px; }
  .industry-carousel { position: relative; overflow: hidden; margin: 0 auto; max-width: 1100px;
    -webkit-mask-image: linear-gradient(90deg, transparent 0, #000 4%, #000 96%, transparent 100%);
    mask-image: linear-gradient(90deg, transparent 0, #000 4%, #000 96%, transparent 100%); }
  .industry-track { display: flex; gap: 18px; width: max-content; animation: industry-scroll 30s linear infinite; }
  .industry-carousel:hover .industry-track { animation-play-state: paused; }
  @keyframes industry-scroll { from { transform: translateX(0); } to { transform: translateX(-50%); } }
  .industry-card {
    display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 14px;
    flex: 0 0 auto; width: 170px; height: 158px;
    padding: 26px 14px 22px;
    border: 1px solid #e8eef5;
    border-radius: 16px;
    background: #fff;
    box-shadow: 0 2px 8px rgba(13,23,32,.04);
    text-align: center;
    position: relative; overflow: hidden;
    transition: transform .22s ease, box-shadow .22s ease, border-color .22s ease;
    cursor: default;
  }
  .industry-card::after {
    content: '';
    position: absolute;
    bottom: 0; left: 0; right: 0; height: 3px;
    background: linear-gradient(90deg, var(--from), var(--to));
    transform: scaleX(0);
    transform-origin: center;
    transition: transform .25s ease;
    border-radius: 0 0 2px 2px;
  }
  .industry-card:hover { transform: translateY(-5px); box-shadow: 0 16px 40px rgba(0,0,0,.10); border-color: rgba(0,0,0,.06); }
  .industry-card:hover::after { transform: scaleX(1); }
  .card-icon-wrap {
    width: 58px; height: 58px; flex-shrink: 0;
    display: grid; place-items: center;
    border-radius: 15px;
    background: var(--bg);
    color: #fff;
    box-shadow: 0 6px 18px rgba(0,0,0,.22);
    transition: transform .22s ease, box-shadow .22s ease;
  }
  .industry-card:hover .card-icon-wrap {
    transform: scale(1.12) rotate(-5deg);
    box-shadow: 0 10px 28px rgba(0,0,0,.32);
  }
  .industry-card strong { color: #1e2d45; font-size: .82rem; font-weight: 600; line-height: 1.25; text-align: center; }
  .process-section { padding: 92px 0 100px; background: white; }
  .process-heading { margin-bottom: 42px; text-align: center; }
  .process-heading span { display: block; margin-bottom: 7px; color: #0a5df0; font-size: .66rem; font-weight: 700; letter-spacing: .09em; text-transform: uppercase; }
  .process-heading h2 { font-size: clamp(1.85rem, 3vw, 2.85rem); }
  .process-rail { position: relative; display: grid; grid-template-columns: repeat(5, 1fr); gap: 28px; }
  .process-rail::before { position: absolute; height: 1px; left: 8%; right: 8%; top: 42px; border-top: 1px dashed #c7d3df; content: ""; }
  .process-step { position: relative; z-index: 1; display: flex; flex-direction: column; align-items: center; text-align: center; }
  .process-icon { width: 78px; height: 78px; display: grid; place-items: center; margin-bottom: 16px; border-radius: 50%; color: #7354c8; background: #f0edff; box-shadow: inset 0 0 0 1px rgba(115,84,200,.12), 0 12px 30px rgba(13,23,32,.05); }
  .process-step.green .process-icon { color: #168d53; background: #e9f7ef; }
  .process-step.blue .process-icon { color: #2471c8; background: #eaf3ff; }
  .process-step.orange .process-icon { color: #d66b2f; background: #fff0e6; }
  .process-step.navy .process-icon { color: #1f63b8; background: #eef5ff; }
  .process-step h3 { font-size: .98rem; font-weight: 700; letter-spacing: -.02em; }
  .process-step p { max-width: 190px; margin-top: 10px; color: var(--muted); font-size: .78rem; font-weight: 500; line-height: 1.6; }
  .why-section { padding: 90px 0 100px; background: linear-gradient(160deg, #0a1120 0%, #0d1628 50%, #0a1120 100%); position: relative; overflow: hidden; }
  .why-section::before { position: absolute; width: 500px; height: 500px; right: -100px; top: -100px; border-radius: 50%; background: radial-gradient(circle, rgba(59,130,246,.12) 0%, transparent 65%); content: ""; pointer-events: none; }
  .why-section::after { position: absolute; width: 400px; height: 400px; left: -120px; bottom: -80px; border-radius: 50%; background: radial-gradient(circle, rgba(233,113,66,.08) 0%, transparent 65%); content: ""; pointer-events: none; }
  .why-header { position: relative; z-index: 1; max-width: 720px; margin-bottom: 50px; }
  .why-header h2 { color: #f1f5f9; font-size: clamp(2rem, 3.5vw, 3rem); letter-spacing: -.03em; }
  .why-header .lead { color: #94a3b8; margin-top: 20px; }
  .why-bento { position: relative; z-index: 1; display: grid; grid-template-columns: repeat(3, 1fr); gap: 18px; }
  .why-card { position: relative; padding: 32px 28px; border: 1px solid rgba(255,255,255,.08); border-radius: 18px; background: rgba(255,255,255,.03); transition: transform .3s ease, border-color .3s ease, background .3s ease; overflow: hidden; }
  .why-card:hover { transform: translateY(-4px); border-color: rgba(255,255,255,.18); background: rgba(255,255,255,.06); }
  .why-icon { display: grid; place-items: center; width: 52px; height: 52px; border-radius: 14px; margin-bottom: 18px; }
  .why-card h3 { color: #f1f5f9; font-size: 1.1rem; font-weight: 700; }
  .why-card p { margin-top: 10px; color: #94a3b8; font-size: .88rem; line-height: 1.6; }
  .why-num { position: absolute; top: 16px; right: 22px; color: rgba(255,255,255,.07); font-size: 2.5rem; font-weight: 800; }
  .why-card-blue .why-icon { background: rgba(59,130,246,.15); color: #60a5fa; }
  .why-card-purple .why-icon { background: rgba(168,85,247,.15); color: #c084fc; }
  .why-card-teal .why-icon { background: rgba(20,184,166,.15); color: #2dd4bf; }
  .why-card-green .why-icon { background: rgba(34,197,94,.15); color: #4ade80; }
  .why-card-orange .why-icon { background: rgba(249,115,22,.15); color: #fb923c; }
  .why-card-pink .why-icon { background: rgba(236,72,153,.15); color: #f472b6; }
  @media (max-width: 1100px) {
    .work-grid { grid-template-columns: repeat(3, 1fr); }
    .process-rail { grid-template-columns: repeat(3, 1fr); gap: 24px; }
    .process-rail::before { display: none; }
    .process-step { min-height: 160px; }
    .solution-grid { grid-template-columns: repeat(2, 1fr); }
  }
  @media (max-width: 960px) {
    .hero-grid { grid-template-columns: 1fr; }
    .hero-copy { max-width: 780px; }
    .hero :global(.ecosystem) { max-width: 560px; }
    .leadership-panel { grid-template-columns: repeat(2,1fr); }
    .leadership-item:nth-child(3) { border-left: 0; border-top: 1px solid var(--border); }
    .leadership-item:nth-child(4) { border-top: 1px solid var(--border); }
    .industry-heading { align-items: start; flex-direction: column; }
    .process-rail { grid-template-columns: repeat(2, 1fr); gap: 24px; }
    .process-rail::before { display: none; }
    .process-step { min-height: 150px; }
    .why-bento { grid-template-columns: repeat(2, 1fr); }
  }
  @media (max-width: 680px) {
    .hero { padding-top: 132px; }
    .hero-copy h1 { font-size: clamp(2.55rem, 11vw, 3.45rem); line-height: 1.03; }
    .leadership-panel { grid-template-columns: 1fr; }
    .leadership-item, .leadership-item:first-child { border-top: 1px solid var(--border); border-left: 0; }
    .leadership-item:first-child { border-top: 0; }
    .solution-grid { grid-template-columns: 1fr; }
    .featured-work-section { padding: 40px 0 56px; overflow: hidden; }
    .compact-heading { align-items: start; flex-direction: column; }
    .compact-heading h2 { font-size: clamp(1.55rem, 7vw, 2.05rem); }
    .industry-heading { gap: 22px; }
    .work-grid {
      display: flex;
      gap: 14px;
      overflow-x: auto;
      overflow-y: hidden;
      padding: 2px 0 16px;
      scroll-snap-type: x mandatory;
      scrollbar-width: none;
      -webkit-overflow-scrolling: touch;
    }
    .work-grid::-webkit-scrollbar { display: none; }
    .work-card { flex: 0 0 min(76vw, 285px); scroll-snap-align: start; }
    .work-art { height: 118px; }
    .work-content { min-height: 178px; }
    .why-bento { grid-template-columns: 1fr; }
    .process-rail { grid-template-columns: 1fr; gap: 22px; }
    .process-step { min-height: 0; display: grid; grid-template-columns: 64px 1fr; align-items: center; column-gap: 16px; text-align: left; }
    .process-icon { width: 64px; height: 64px; margin: 0; }
    .process-icon svg { width: 24px; height: 24px; }
    .process-step p { max-width: 520px; }
  }
  @media (max-width: 430px) {
    .work-card { flex-basis: 82vw; }
  }
</style>
