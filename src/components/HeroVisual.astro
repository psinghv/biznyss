<div class="dash-wrap" aria-label="Growth Intelligence Platform dashboard showing AI-driven revenue growth of 142%, 38 active AI campaigns, 12,840 monthly automations, and rising channel performance">

  <div class="dash-glow"></div>

  <!-- Main dashboard card -->
  <div class="dash-card">

    <!-- Title bar -->
    <div class="dash-bar">
      <div class="dots"><i class="d-red"></i><i class="d-yellow"></i><i class="d-green"></i></div>
      <span class="dash-name">Growth Intelligence Platform</span>
      <span class="dash-live"><em class="live-dot"></em>Live</span>
    </div>

    <!-- Primary metric + sparkline -->
    <div class="primary-row">
      <div class="primary-left">
        <span class="ai-badge">
          <svg viewBox="0 0 14 14" fill="none" aria-hidden="true"><circle cx="7" cy="7" r="6" stroke="currentColor" stroke-width="1.2"/><path d="M4.5 7h5M7 4.5v5" stroke="currentColor" stroke-width="1.2" stroke-linecap="round"/></svg>
          AI-Powered
        </span>
        <div class="big-number">+142%</div>
        <div class="big-label">Revenue Growth · Year over Year</div>
        <div class="chips">
          <span class="chip green">
            <svg viewBox="0 0 10 10" fill="none" aria-hidden="true"><path d="M2 6 l2.5-3 L7 5.5" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round"/></svg>
            MRR $2.4M
          </span>
          <span class="chip blue">38 AI Campaigns</span>
        </div>
      </div>
      <div class="primary-right">
        <svg class="sparkline" viewBox="0 0 200 72" preserveAspectRatio="none" aria-hidden="true">
          <defs>
            <linearGradient id="sgFill" x1="0" y1="0" x2="0" y2="1">
              <stop offset="0%" stop-color="#3b82f6" stop-opacity="0.35"/>
              <stop offset="100%" stop-color="#3b82f6" stop-opacity="0"/>
            </linearGradient>
          </defs>
          <path class="spark-area" d="M0,62 C18,58 28,52 44,46 S62,38 80,32 S100,22 120,20 S142,13 162,9 S185,4 200,2 L200,72 L0,72 Z" fill="url(#sgFill)"/>
          <path class="spark-line" d="M0,62 C18,58 28,52 44,46 S62,38 80,32 S100,22 120,20 S142,13 162,9 S185,4 200,2" fill="none" stroke="#3b82f6" stroke-width="2" stroke-linecap="round"/>
          <circle cx="200" cy="2" r="3.5" fill="#3b82f6"/>
          <circle cx="200" cy="2" r="6" fill="#3b82f6" fill-opacity="0.2"/>
        </svg>
        <span class="spark-label">12-month trend</span>
      </div>
    </div>

    <!-- KPI cards row -->
    <div class="kpi-row">
      <div class="kpi-card">
        <span class="kpi-icon green">
          <svg viewBox="0 0 18 18" fill="none" aria-hidden="true"><path d="M4 9a5 5 0 0 1 9.33-2.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/><path d="M14 6 l-1.5 2.5 L10 7" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/><path d="M14 9a5 5 0 0 1-9.33 2.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/><path d="M4 12 l1.5-2.5 L8 11" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </span>
        <strong>12,840</strong>
        <span>Automations / mo</span>
      </div>
      <div class="kpi-card">
        <span class="kpi-icon orange">
          <svg viewBox="0 0 18 18" fill="none" aria-hidden="true"><circle cx="9" cy="9" r="7" stroke="currentColor" stroke-width="1.5"/><circle cx="9" cy="9" r="3.5" stroke="currentColor" stroke-width="1.5"/><circle cx="9" cy="9" r="1" fill="currentColor"/></svg>
        </span>
        <strong>38</strong>
        <span>AI Campaigns</span>
      </div>
      <div class="kpi-card">
        <span class="kpi-icon blue">
          <svg viewBox="0 0 18 18" fill="none" aria-hidden="true"><path d="M3 13 l3-4 3 2 3-5 3 3" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </span>
        <strong>94</strong>
        <span>Client NPS</span>
      </div>
    </div>

    <!-- Channel performance -->
    <div class="channels">
      <div class="ch-hdr"><span>Marketing Channel Performance</span><span class="ch-period">Last 30 days</span></div>
      <div class="ch-item">
        <span class="ch-name">AI-driven Paid</span>
        <div class="ch-track"><div class="ch-fill blue" style="width:72%"></div></div>
        <span class="ch-pct">72%</span>
      </div>
      <div class="ch-item">
        <span class="ch-name">Organic / SEO</span>
        <div class="ch-track"><div class="ch-fill green" style="width:58%"></div></div>
        <span class="ch-pct">58%</span>
      </div>
      <div class="ch-item">
        <span class="ch-name">Performance CRM</span>
        <div class="ch-track"><div class="ch-fill orange" style="width:44%"></div></div>
        <span class="ch-pct">44%</span>
      </div>
    </div>
  </div>

  <!-- Floating cards -->
  <div class="float float-tl">
    <span class="float-ico">
      <svg viewBox="0 0 16 16" fill="none" aria-hidden="true"><path d="M2 8 l3.5 3.5 L14 4" stroke="#10b981" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
    </span>
    <div class="float-text">
      <strong>Campaign Live</strong>
      <span>AI optimised · just now</span>
    </div>
  </div>

  <div class="float float-br">
    <div class="float-stat">+89%</div>
    <span>Ops Automation</span>
  </div>

</div>

<style>
  /* ── Wrapper ─────────────────────────────────────────────── */
  .dash-wrap {
    position: relative;
    width: 100%; max-width: 590px;
    margin-inline: auto;
    padding: 36px 20px 36px 12px;
  }
  .dash-glow {
    position: absolute;
    inset: 10% 5%;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(59,130,246,.28) 0%, rgba(6,182,212,.1) 45%, transparent 70%);
    filter: blur(32px);
    pointer-events: none;
  }

  /* ── Main card ───────────────────────────────────────────── */
  .dash-card {
    position: relative; z-index: 2;
    border-radius: 20px;
    border: 1px solid rgba(255,255,255,.11);
    background: rgba(8,18,36,.92);
    backdrop-filter: blur(20px);
    box-shadow: 0 32px 80px rgba(0,0,0,.45), inset 0 1px 0 rgba(255,255,255,.08);
    overflow: hidden;
  }

  /* Title bar */
  .dash-bar {
    display: flex; align-items: center; gap: 10px;
    padding: 13px 18px;
    border-bottom: 1px solid rgba(255,255,255,.07);
    background: rgba(255,255,255,.03);
  }
  .dots { display: flex; gap: 5px; flex-shrink: 0; }
  .dots i { width: 9px; height: 9px; border-radius: 50%; }
  .d-red    { background: #ff5f57; }
  .d-yellow { background: #ffbd2e; }
  .d-green  { background: #28ca41; }
  .dash-name { flex: 1; font-size: .72rem; font-weight: 600; color: rgba(255,255,255,.5); letter-spacing: .02em; }
  .dash-live { display: flex; align-items: center; gap: 5px; font-size: .68rem; font-weight: 600; color: #10b981; }
  .live-dot {
    width: 6px; height: 6px; border-radius: 50%; background: #10b981;
    box-shadow: 0 0 0 3px rgba(16,185,129,.25);
    animation: pulse 1.8s ease-in-out infinite;
  }
  @keyframes pulse {
    0%,100% { box-shadow: 0 0 0 3px rgba(16,185,129,.25); }
    50% { box-shadow: 0 0 0 6px rgba(16,185,129,.08); }
  }

  /* Primary metric row */
  .primary-row {
    display: flex; align-items: flex-start; gap: 16px;
    padding: 20px 18px 16px;
    border-bottom: 1px solid rgba(255,255,255,.06);
  }
  .primary-left { flex: 1; min-width: 0; }
  .ai-badge {
    display: inline-flex; align-items: center; gap: 5px;
    padding: 4px 10px; border-radius: 999px;
    font-size: .65rem; font-weight: 700; letter-spacing: .04em;
    color: #60a5fa; background: rgba(96,165,250,.12); border: 1px solid rgba(96,165,250,.22);
    margin-bottom: 10px;
  }
  .ai-badge svg { width: 11px; height: 11px; }
  .big-number {
    font-size: clamp(2rem, 4vw, 2.55rem);
    font-weight: 800; letter-spacing: -.04em; line-height: 1;
    color: #f1f5f9;
  }
  .big-label { margin-top: 5px; font-size: .7rem; color: rgba(255,255,255,.4); font-weight: 500; }
  .chips { display: flex; flex-wrap: wrap; gap: 6px; margin-top: 14px; }
  .chip {
    display: inline-flex; align-items: center; gap: 4px;
    padding: 4px 10px; border-radius: 999px; font-size: .65rem; font-weight: 700;
  }
  .chip svg { width: 9px; height: 9px; }
  .chip.green { color: #10b981; background: rgba(16,185,129,.12); border: 1px solid rgba(16,185,129,.2); }
  .chip.blue  { color: #60a5fa; background: rgba(96,165,250,.12);  border: 1px solid rgba(96,165,250,.2); }

  /* Sparkline */
  .primary-right { flex-shrink: 0; width: 42%; display: flex; flex-direction: column; gap: 4px; }
  .sparkline { width: 100%; height: 72px; display: block; }
  .spark-label { font-size: .6rem; color: rgba(255,255,255,.3); text-align: right; font-weight: 500; }

  /* KPI cards row */
  .kpi-row {
    display: grid; grid-template-columns: repeat(3,1fr);
    border-bottom: 1px solid rgba(255,255,255,.06);
  }
  .kpi-card {
    display: flex; flex-direction: column; align-items: flex-start; gap: 4px;
    padding: 14px 14px 16px;
    border-left: 1px solid rgba(255,255,255,.06);
  }
  .kpi-card:first-child { border-left: 0; }
  .kpi-icon {
    width: 30px; height: 30px; border-radius: 8px;
    display: grid; place-items: center; margin-bottom: 4px;
  }
  .kpi-icon svg { width: 16px; height: 16px; }
  .kpi-icon.green  { background: rgba(16,185,129,.14);  color: #10b981; }
  .kpi-icon.orange { background: rgba(249,115,22,.14);  color: #f97316; }
  .kpi-icon.blue   { background: rgba(96,165,250,.14);  color: #60a5fa; }
  .kpi-card strong { font-size: 1.08rem; font-weight: 800; color: #f1f5f9; letter-spacing: -.02em; }
  .kpi-card span   { font-size: .62rem; color: rgba(255,255,255,.4); font-weight: 500; }

  /* Channels */
  .channels { padding: 14px 18px 18px; }
  .ch-hdr {
    display: flex; justify-content: space-between; align-items: center;
    margin-bottom: 12px;
  }
  .ch-hdr > span:first-child { font-size: .68rem; font-weight: 700; color: rgba(255,255,255,.55); letter-spacing: .02em; }
  .ch-period { font-size: .6rem; color: rgba(255,255,255,.28); font-weight: 500; }
  .ch-item { display: flex; align-items: center; gap: 10px; margin-bottom: 8px; }
  .ch-item:last-child { margin-bottom: 0; }
  .ch-name { flex-shrink: 0; width: 118px; font-size: .65rem; color: rgba(255,255,255,.5); font-weight: 500; }
  .ch-track { flex: 1; height: 5px; border-radius: 999px; background: rgba(255,255,255,.07); overflow: hidden; }
  .ch-fill  { height: 100%; border-radius: 999px; transition: width .6s ease; }
  .ch-fill.blue   { background: linear-gradient(90deg, #3b82f6, #60a5fa); }
  .ch-fill.green  { background: linear-gradient(90deg, #059669, #10b981); }
  .ch-fill.orange { background: linear-gradient(90deg, #ea580c, #f97316); }
  .ch-pct { flex-shrink: 0; width: 30px; text-align: right; font-size: .63rem; font-weight: 700; color: rgba(255,255,255,.45); }

  /* ── Floating cards ──────────────────────────────────────── */
  .float {
    position: absolute; z-index: 5;
    display: flex; align-items: center; gap: 10px;
    padding: 10px 14px; border-radius: 14px;
    border: 1px solid rgba(255,255,255,.13);
    background: rgba(8,18,36,.92);
    backdrop-filter: blur(14px);
    box-shadow: 0 16px 48px rgba(0,0,0,.4);
    animation: floatY 4s ease-in-out infinite;
  }
  @keyframes floatY {
    0%,100% { transform: translateY(0); }
    50%      { transform: translateY(-6px); }
  }
  .float-tl { top: 10px; left: -8px; animation-delay: 0s; }
  .float-br { bottom: 10px; right: -4px; animation-delay: 2s; flex-direction: column; align-items: flex-start; gap: 2px; }

  .float-ico {
    width: 28px; height: 28px; flex-shrink: 0; border-radius: 8px;
    background: rgba(16,185,129,.15); display: grid; place-items: center;
  }
  .float-ico svg { width: 14px; height: 14px; }
  .float-text strong { display: block; font-size: .67rem; font-weight: 700; color: #f1f5f9; }
  .float-text span   { font-size: .6rem; color: rgba(255,255,255,.4); }

  .float-stat { font-size: 1.12rem; font-weight: 800; color: #f97316; letter-spacing: -.02em; line-height: 1; }
  .float-br > span   { font-size: .62rem; color: rgba(255,255,255,.4); font-weight: 500; }

  /* ── Responsive ─────────────────────────────────────────── */
  @media (max-width: 900px) {
    .dash-wrap { padding: 24px 12px; }
    .primary-right { display: none; }
    .primary-left  { width: 100%; }
  }
  @media (max-width: 540px) {
    .ch-name { width: 90px; }
    .float-tl { display: none; }
    .float-br { display: none; }
  }
</style>
